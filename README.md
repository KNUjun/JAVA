# JAVA
### `2019/01` java lecture code

## CH01
### 1. Celsius2Fahrenheit.java
Celsius 를 Fahrenhiet degrree 로 바꾸는 code를 작성한다.
  
## CH02
### 1. CircleArea.java
`user`로 부터 `radius`를 받는다.  
입력받은 `radius`를 통해 원의 `Area`를 계산해서 출력한다.  
### 2. Constant.java
`final`의 이해와 사용에 대해 알아본다.
### 3. Salary.java
월급을 입력받아서 연봉을 계산해 출력한다.
  
## CH03
### 1. ComparingNumbers.java
각 자료형들간의 비교연산의 결과를 알아본다.
### 2. DaysInMonth.java
입력한 `month`의 `day`를 출력한다.
### 3. Ex32_equals.java
`String` 내장 함수인 `equals`에 대해 알아본다.
### 4. Metropolis.java
`user`로부터 `capital`, `Population`, `Bourgeois`에 대한 정보를 입력받아 도시가 `Metropolis`인지 출력한다.
### 5. Tax.java
수입별 `Tax`을 계산해서 출력한다.

## CH04
### 1. ComputeAngles.java
세 점을 입력받아 세 점이 이루는 삼각형의 내각을 각각 출력한다.
### 2. math_exercises.java
`Math`에 저장된 함수들에 대해 알아본다.
### 3. RandomNumberGen.java
`Math.random` 함수를 이용해 원하는 범위의 정수를 출력한다.
### 4. CountingSpaces.java
사용자로 부터 문자열을 입력받아 공백(`' '`)의  수를 센다.
### 5. PythagoreanAngles.java
`cos 제 2법칙`을 이용해 세 점이 이루는 삼각형의 내각을 각각 출력한다.
  
## CH05
### 1. BreakTest.java
`user`로부터 `score`를 입력받는다. 음수를 입력받으면 반복을 멈추고 `score`들의 평균값을 출력한다.
### 2. DiamondStar.java
입력한 `n`에 맞는 `DiamondStar` 를 출력한다.
### 3. DIamondStar2.java
입력한 `n`에 맞는 `DiamondStar2` 를 출력한다.
### 4. Factorial10.java
입력한 `n`에 맞는 `n!`(factorial)을 계산해서 출력한다.
### 5. LCM.java
유클리드 호제법을 이용해 두 정수의 `GCD`와 `LCM`을 찾는다.
### 6. TwoTriangles.java
입력한 `n`에 맞는 `TwoTriangles`를 출력한다.

## CH06
### 1. TestRandomCharacter.java
알파벳 대소문자, 숫자 중에서 random하게 문자를 생성하여 출력한다.
### 2. RandomCharacter.java
`TestRandomCharacter.java`에서 실질적으로 random하게 문자를 생성하는 역할을 한다.
### 3. VariableNameRandomGen.java
숫자, 알파벳 대소문자, _ , $ 중에서 하나의 문자를 생성한다.
### 4. TestPassByValue.java
숫자의 swap을 실행해본다.
### 5. TestMethodOverloading.java
`overload`에 대해 이해하고 실행해본다.

## CH07
### 1. ArrayTest.java
`Array`에 대해 이해한다.
### 2. AverageRandom.java
1~9 사이의 정수를 random하게 `n`개 생성하여 평균값을 출력한다.
### 3. FindNearest.java
주어진 점들 사이에서 가장 거리가 가까이 있는 두 점을 출력한다.
### 4. MultipleChoice.java
학생들의 각 문항별 답안을 답과 비교해서 각 학생별로 정답의 수를 출력한다.
### 5. RandPerm.java
1부터 n까지의 정수를 random하게 `array`에 저장시킨다.
### 6. SortedRandom.java
`array`에 저장되어있는 데이터들을 random하게 배열시킨다.

## CH09
### 1. BMI2.java
객체를 이용해 주어진 `weight`와 `height`로부터 `BMI`를 계산한다.
### 2. UseBMI1.java
객체를 이용해 BMI를 계산한다.
### 3. UseBMI2.java
BMI2.java를 이용해 BMI를 계산한다.
### 4. CircleWithStaticMembers.java
클래스의 `static` 을 이해해서 instance 변수들 사이에 값을 공유할 수 있도록 한다.
### 5. TestCircleWithStaticMembers.java
CircleWithStaticMembers.java 를 활용해본다.
### 6. Course.java
학생이 수강하고 있는 `course`에 대한 정보를 객체에 저장한다.
### 7. UseCourse.java
`Course`.java를 이용한 예제를 실행해본다.
### 8. TV2.java
객체를 이용해 `TV`의 동작을 구현해본다.
### 9. UseTV2.java
TV2.java를 통해 구현된 TV를 UseTV2.java를 이용해 테스트해본다.

## CH11
### 1. CastingDemo2.java
각각의 도형에 주어진 정보들을 통해 도형의 넓이를 출력한다.
### 2. LargePow2.java
`BigInteger`을 이용해 2의 지수승을 구한다.
### 3. ReadData.java
`File`을 통한 입력을 이해해본다.
### 4. WriteData.java
`File`을 통항 출력을 이해해본다.
### 5. ./geometric/GeometricObject.java
기하학적 도형에 대한 객체를 생성한다.
### 6. ./geometric/Circle.java
`GeometricObject.java`를 상속받는 `Circle` 클래스를 작성한다. 각 도형의 정보를 `override`하여 circle에 맞도록 바꾼다.
### 7. ./geometric/Rectangle.java
`GeometricObject.java`를 상속받는 `Rectangle` 클래스를 작성한다. 각 도형의 정보를 `override`하여 rectangle에 맞도록 바꾼다.
### 8. TestCircleRectangle.java
`geometric` 폴더에 `package`되어 있는 java파일들을 `import`하여 circle과 rectangle에 대해 테스트해본다.
### 9. TestWrongOverriding.java
상속받은 메소드를 잘 못 `override`한 경우를 알아본다.
### 10. Test_toString.java
클래스의 `.toString()` 메소드의 동작에 대해 알아본다.

## CH12
### 1. CountWords.java
주어진 URL을 Stream으로 하여 웹사이트의 글을 파싱한다. 읽어들인 토큰들로부터 몇개의 단어가 있는지 세서 출력한다.
### 2. FindStatics.java
`target file`로부터 정수들을 읽어들여 분석한 통계값들을 출력한다.
### 3. TestCircleWithRadiusException.java
`Exception`에대해 이해하고 활용해본다.
### 4. ToggleCases.java
주어진 URL로 부터 글을 읽어들여, 대문자를 소문자로, 소문자를 대문자로 변경하여 출력한다.
### 5. WriteDataWithAutoClose2.java
`file`의 개방과 닫기를 자동으로 해주는 `try`문에 대해 알아본다.

## CH13
### 1. TestBuyer.java
`Product` 클래스를 상속받는 `Tv`, `Computer`, `Audio` 객체 들을 활용해 `Buyer`의 동작을 알아본다.
### 2. TestMyTV0.java
상속을 활용한 `TV`의 동작을 알아본다.
### 3. TestShape.java
`Quadrangle` 클래스는 `shape`을 상속받고 있고, `Quadrangle` 클래스를 상속받는 여러 객체들의 동작을 알아본다.

## HW01
### 1. ApproximatePISphere.java
몬테 카를로 방식을 이용해 원주율을 근사적으로 계산해본다.
### 2. EncryptMult.java
암호화된 숫자들을 `inverse modulo`를 이용해 복호화한다.
### 3. SimpleComplex.java
복소수를 구현한다.
### 4. TestSimpleComplex.java
SimpleComplex.java를 통해 구현한 복소수를 테스트해본다.
### 5. StemCosine.java
`Cosine`함수를 그래프로 나타내본다.
### 6. StemSine.java
`Sine`함수를 그래프로 나타내본다.
### 7. StemTriangle1.java
삼각형을 출력한다.
### 8. StemTriangle2.java
삼각형을 출력한다.

## HW02
### 1. Complex.java
복소수 구현과 내장 함수들을 정의한다.
### 2. GenricMatrix.java
`Matrix` 연산을 `Generic`을 이용하여 구현한다.
### 3. DoubleMatrix.java
`double` 타입을 element로 가지는 Matrix의 연산을 정의한다.
### 4. ComplexMarix.java
`complex` 타입을 element로 가지는 Matrix의 연산을 정의한다.
### 5. IntegerMatrix.java
`int` 타입을 element로 가지는 Matrix의 연산을 정의한다.
### 6. TestMySetInt.java
`int` 타입을 element로 가지는 `Set`의 구현과 연산을 정의한다.
